{
  "name": "oeconomia-bridge-contracts",
  "version": "0.1.0",
  "description": "Oeconomia Cross-Chain Bridge - security-first lock-and-mint bridge (pre-audit build)",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:bscTestnet": "hardhat run scripts/deploy.ts --network bscTestnet",
    "validator": "tsx offchain/validator.ts",
    "relayer": "tsx offchain/relayer.ts",
    "monitor": "tsx offchain/monitor.ts"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.22.0",
    "tsx": "^4.23.11",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "ethers": "^6.17.0"
  }
}
